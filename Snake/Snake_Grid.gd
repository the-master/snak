extends TileMap

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
#	var randx = randi() % 626 + 16
#	var randy = randi() % 626 + 16
#	var pos = world_to_map(Vector2(randx, randy)) * 32
#	$Apple.position.x = pos.x + 16
#	$Apple.position.y = pos.y + 16
